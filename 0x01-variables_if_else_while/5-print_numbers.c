#include <stdio.h>

/**
 * main - This program prints all single digit numbers
 *
 * Return: 0 if successful
 */
int main(void)
{
	int x;

	for (x = 0 ; x < 10 ; x++)
		printf("%i", x);
	printf("\n");
	return (0);
}
